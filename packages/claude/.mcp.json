{
  "mcpServers": {
    "nrev-workflows": {
      "command": "uv",
      "args": [
        "run",
        "--quiet",
        "--project",
        "${CLAUDE_PLUGIN_ROOT}/mcp",
        "nrev-workflows-mcp"
      ]
    }
  }
}
